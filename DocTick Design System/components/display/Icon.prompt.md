Lucide ikon sarmalayıcısı; renk `currentColor`'dan gelir.

```jsx
<Icon name="calendar" size={16}/>
<span style={{color:'var(--brand)'}}><Icon name="check"/></span>
```

Set: calendar, clock, chevron-right/left, mail, user, plus, trash, pencil, check, x, bell, star, grid, logout.
